#include <stdio.h>

    int main(void) {

        const int length = 20;
        const int width = 40;

        int rectangle = length * width;

        printf("Rectangle Area: %d\n", rectangle);


        return 0;
    }